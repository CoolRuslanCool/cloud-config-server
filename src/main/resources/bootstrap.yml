spring:
  profiles:
    active: develop
  application:
    name: config
  cloud:
    config:
      server:
        git.uri: https://github.com/CoolRuslanCool/cloud-properties.git
        git.searchPaths: server-config
  security:
    user:
      name: root
      password: root